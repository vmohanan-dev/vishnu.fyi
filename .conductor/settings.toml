"$schema" = "https://conductor.build/schemas/settings.repo.schema.json"

[scripts]
setup = "npm_config_registry=https://registry.npmjs.org npm ci"
run = "npm_config_registry=https://registry.npmjs.org npm run dev -- --port $CONDUCTOR_PORT"
run_mode = "concurrent"
